# Reviewed native dependency key/value data. Changes require a security review.
# Parse with lock.sh; this file is intentionally not sourced as shell code.
WIMLIB_VERSION=1.14.5
WIMLIB_SOURCE=wimlib-1.14.5.tar.gz
WIMLIB_SOURCE_URL=https://wimlib.net/downloads/wimlib-1.14.5.tar.gz
# Populate from the upstream release checksum. CI and build.sh fail closed when
# this is empty or is not exactly 64 lowercase hexadecimal characters.
WIMLIB_SOURCE_SHA256=84221a3abd5b91228f15f8e6065c335a336237b5738197b75bf419eea561a194
LINUX_TOOLCHAIN=ubuntu-24.04-clang-18
MACOS_TOOLCHAIN=xcode-16.4-apple-clang
# Space-delimited long options; lock.sh validates and converts these to an array.
CONFIGURE_FLAGS=--disable-static --enable-shared --without-ntfs-3g --without-fuse
